<resources>
    <!-- Self-contained: works regardless of the host app's own theme, so a Material
         widget crash in the host's theme can never take this screen down with it. -->
    <style name="Theme.NiimbotPrintSdk" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">#1A73E8</item>
        <item name="colorPrimaryDark">#0F4C9C</item>
        <item name="colorAccent">#1A73E8</item>
    </style>
</resources>
